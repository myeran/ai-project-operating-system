# מדריך משתמש — AI Project Operating System

## מה מטרת הפרויקט?

המערכת עוזרת למשתמש לנהל פרויקט בצורה מסודרת, משלב הרעיון ועד למידה
והשלמה. היא שומרת את מצב הפרויקט, מציגה את השלב הפעיל, מפעילה את ה־Skill
המתאים ומנחה את המשתמש מה צריך להשלים לפני המעבר לשלב הבא.

המסלול המרכזי הוא:

`Discovery → Strategy → Planning → Design → Execution → Validation → Launch → Learning`

המערכת אינה מקבלת החלטות במקום המשתמש. ה־Skills מציעים ניתוח, תוכניות,
בדיקות ותוצרים; המשתמש מאשר החלטות, Scope ומעברים בין שלבים.

## איך מפעילים?

### 1. הורדת המאגר

אפשר לשמור את המאגר בכל תיקייה במחשב — למשל Documents, Desktop או תיקייה
ייעודית לפרויקטים. אין חובה להשתמש ב־`Documents/Codex`.

אפשר להוריד באמצעות:

```bash
git clone https://github.com/myeran/ai-project-operating-system.git
```

המאגר ייווצר בתיקייה שבה הורצה הפקודה. אפשר גם לבקש מ־Codex:

```text
הורד את המאגר הבא למחשב שלי:
https://github.com/myeran/ai-project-operating-system.git

שמור אותו בתיקייה מקומית שאבחר.
אל תשנה קבצים בשלב זה.
לאחר ההורדה, הצג לי את הנתיב המלא לתיקייה.
```

### 2. פתיחת המאגר ב־Codex

ב־Codex בחר **Open Folder** או **Add Project**, ובחר את התיקייה הראשית
`ai-project-operating-system` שאליה הורדת את המאגר.

זו התיקייה שבה נמצאים:

```text
README.md
runtime
.codex
03_Skill_Registry
```

אין לבחור את תיקיית `runtime` או תיקיית משנה אחרת.

### 3. הפעלת המערכת

ב־macOS לחץ פעמיים על `Start AI Project OS.command`. לחלופין, מתוך תיקיית
המאגר הפעל:

```bash
./Start\ AI\ Project\ OS.command
```

המערכת תפעיל את ה־Runtime, את שכבת ה־MCP ואת ה־Local Chat ותפתח את ממשק
הצ׳אט בכתובת `http://127.0.0.1:8790`.

כדי לעצור, לחץ פעמיים על `Stop AI Project OS.command`.

## מה כותבים בצ׳אט?

### הפעלה ראשונה — Project Navigator

ב־Codex, כאשר הפרויקט פתוח כתיקיית העבודה, אפשר להפעיל את ה־Codex Skill
המותקן באמצעות:

```text
$project-navigator
```

אם ה־Skill אינו מופיע, פתח מחדש את Codex או התקן את תיקיית
`.codex/skills/project-navigator` מתוך המאגר תחת `~/.codex/skills/`.

העתק לצ׳אט את ההודעה הבאה:

```text
הפעל את project_navigator_skill.
הצג את כל שלבי בניית הפרויקט לפי הסדר, את הסטטוס של כל שלב,
את השלב הפעיל הנוכחי, איזה Skill צריך להפעיל כדי להתקדם,
ומהם תנאי הקדם לשלב הבא.
```

ה־Navigator הוא read-only: הוא מציג את המצב ואינו משנה את הפרויקט.

### יצירת פרויקט חדש

```text
אני רוצה להתחיל פרויקט חדש.
```

לאחר מכן המערכת תבקש את הפרטים החסרים:

- שם הפרויקט.
- סוג הפרויקט.
- מטרת הפרויקט.
- התוצאה הרצויה.

כל פרויקט חדש יתחיל בברכה:

```text
שלום ובהצלחה בפרוייקט
```

### המשך פרויקט קיים

```text
אני רוצה להמשיך את הפרויקט [שם הפרויקט].
```

### בקשת הכוונה

```text
תנחה אותי בשלב הפעיל.
```

או:

```text
מה חסר כדי לעבור לשלב הבא?
```

## איך מתקדמים?

בכל שלב המשתמש עונה על השאלות, בודק את התוצרים ומאשר אותם. רק לאחר
שהתנאים הושלמו והמעבר אושר, ה־Runtime מציע את השלב הבא.

Skills תומכים כמו Research, Risk Assessment ו־Decision Support מופעלים לפי
צורך בתוך שלבי הפרויקט, ולא מחליפים שלב במסלול הראשי.

## נתונים ובטיחות

נתוני הפרויקטים נשמרים מקומית ב־SQLite ואינם נכללים ב־Repository הציבורי.
המערכת אינה משנה Scope, אינה מאשרת החלטות ואינה מבצעת שחרור או התחייבות
למשאבים ללא אישור מפורש.

## בדיקות למפתחים

```bash
python3 -m unittest discover -s runtime/tests -p 'test_*.py'
```
